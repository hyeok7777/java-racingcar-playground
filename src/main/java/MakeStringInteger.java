public class MakeStringInteger {

    private String strNumber;
    private CheckNumber checkNumber;

    if(checkNumber.isNumberNegative(no)) {

    }

        if(checkNumber.isNumberZero(no)) {
        return ZERO;
    }
        if(checkNumber.isNumberSingle(no)) {
        return Integer.parseInt(no);
    }

        if(checkNumber.isNumberCommaOrColon(no)) {
        strNumbers = no.split(",|:");
    }
}
